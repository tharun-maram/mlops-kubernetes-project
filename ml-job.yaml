apiVersion: batch/v1
kind: Job
metadata:
  name: ml-training-job
spec:
  template:
    spec:
      containers:
      - name: ml-container
        image: tharunmaram/ml-project
        imagePullPolicy: Always
      restartPolicy: Never
  backoffLimit: 2
